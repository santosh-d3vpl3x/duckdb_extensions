name: Publish 🦆duckdb_extension_inet 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_inet/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-inet-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_inet/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-inet-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: inet
    secrets: inherit
