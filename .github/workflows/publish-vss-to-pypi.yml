name: Publish 🦆duckdb_extension_vss 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_vss/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-vss-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_vss/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-vss-to-pypi.yml'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: vss
    secrets: inherit
