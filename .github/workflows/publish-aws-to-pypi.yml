name: Publish 🦆duckdb_extension_aws 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_aws/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-aws-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_aws/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-aws-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: aws
    secrets: inherit
