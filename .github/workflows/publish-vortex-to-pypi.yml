name: Publish 🦆duckdb_extension_vortex 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_vortex/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-vortex-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_vortex/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-vortex-to-pypi.yml'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-test:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: vortex
      supported_platforms: '["osx_arm64","linux_amd64","linux_arm64","osx_amd64"]'  # windows_amd64 is not supported yet
    secrets: inherit

  publish-to-pypi:
    name: Publish 🦆duckdb_extension_vortex 🐍📦 to PyPI
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build-and-test
    runs-on: ubuntu-latest

    environment:
      name: pypi
      url: https://pypi.org/p/duckdb-extension-vortex

    permissions:
      id-token: write

    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          name: duckdb-extension-vortex-distributions
          path: dist/
      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
