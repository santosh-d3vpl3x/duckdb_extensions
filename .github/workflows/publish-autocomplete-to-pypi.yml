name: Publish 🦆duckdb_extension_autocomplete 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_autocomplete/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-autocomplete-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_autocomplete/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-autocomplete-to-pypi.yml'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: autocomplete
    secrets: inherit
