name: Publish 🦆duckdb_extension_avro 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_avro/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-avro-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_avro/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-avro-to-pypi.yml'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: avro
    secrets: inherit
