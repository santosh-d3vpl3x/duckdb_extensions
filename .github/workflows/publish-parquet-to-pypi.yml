name: Publish 🦆duckdb_extension_parquet 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_parquet/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-parquet-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_parquet/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-parquet-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: parquet
    secrets: inherit
