name: Publish 🦆duckdb_extension_encodings 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_encodings/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-encodings-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_encodings/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-encodings-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: encodings
    secrets: inherit
