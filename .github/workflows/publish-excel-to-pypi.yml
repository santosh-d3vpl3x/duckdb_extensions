name: Publish 🦆duckdb_extension_excel 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_excel/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-excel-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_excel/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-excel-to-pypi.yml'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-test:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: excel
    secrets: inherit

  publish-to-pypi:
    name: Publish 🦆duckdb_extension_excel 🐍📦 to PyPI
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build-and-test
    runs-on: ubuntu-latest

    environment:
      name: pypi
      url: https://pypi.org/p/duckdb-extension-excel

    permissions:
      id-token: write

    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          name: duckdb-extension-excel-distributions
          path: dist/
      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
