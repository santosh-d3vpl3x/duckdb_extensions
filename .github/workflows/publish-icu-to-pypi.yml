name: Publish 🦆duckdb_extension_icu 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_icu/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-icu-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_icu/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-icu-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: icu
    secrets: inherit
