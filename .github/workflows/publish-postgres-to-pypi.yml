name: Publish 🦆duckdb_extension_postgres 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_postgres/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-postgres-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_postgres/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-postgres-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: postgres
    secrets: inherit
