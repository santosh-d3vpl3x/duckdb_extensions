name: Publish 🦆duckdb_extension_mysql_scanner 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_mysql_scanner/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-mysql_scanner-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_mysql_scanner/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-mysql_scanner-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: mysql_scanner
    secrets: inherit
