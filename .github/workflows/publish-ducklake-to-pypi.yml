name: Publish 🦆duckdb_extension_ducklake 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_ducklake/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-ducklake-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_ducklake/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-ducklake-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: ducklake
    secrets: inherit
