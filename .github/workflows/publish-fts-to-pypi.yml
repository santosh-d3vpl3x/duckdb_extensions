name: Publish 🦆duckdb_extension_fts 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_fts/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-fts-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_fts/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-fts-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: fts
    secrets: inherit
