name: Publish 🦆duckdb_extension_sqlite 🐍📦 to PyPI

on:
  push:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_sqlite/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-sqlite-to-pypi.yml'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - support_*
    paths:
      - 'extensions/duckdb_extension_sqlite/**'
      - '.github/workflows/_extension-publish.yml'
      - '.github/workflows/publish-sqlite-to-pypi.yml'

jobs:
  publish:
    uses: ./.github/workflows/_extension-publish.yml
    with:
      extension_name: sqlite
    secrets: inherit
